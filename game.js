(async()=>{try{const names=['game.gz.1.txt','game.gz.2.txt','game.gz.3.txt'];const chunks=await Promise.all(names.map(async n=>{const r=await fetch(n);if(!r.ok)throw new Error(`${n} introuvable`);return r.text()}));const b64=chunks.join('').trim();const bin=atob(b64);const bytes=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const code=await new Response(stream).text();const s=document.createElement('script');s.textContent=code;document.head.appendChild(s)}catch(err){console.error('ECHO load error',err);const t=document.getElementById('toast');if(t){t.textContent='Impossible de charger ECHO';t.classList.add('show')}}})();